// Shared types between client and (optional) server code.
// The product/cart/order types live in client/src/lib/catalog.ts,
// client/src/lib/cart.ts, and client/src/lib/orderMessage.ts — import from
// there. This file is kept as a clean extension point for future shared
// types (e.g. once a real backend/admin panel is added).
export {};
